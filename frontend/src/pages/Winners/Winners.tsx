import {TotalCount} from "../../shared/ui/TotalCount/TotalCount.tsx";

export const Winners = ()=>{
    return(
        <div>
            <TotalCount totalNumber={0} name={'Winners'}/>
        </div>
    )
}